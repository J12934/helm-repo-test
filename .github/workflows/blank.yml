name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Publish Helm Charts
      uses: J12934/helm-gh-pages-action@57f3392006624d806f98c77380a189afea394b1e
      with:
          access-token: ${{ secrets.ACCESS_TOKEN }}

